#include<stdio.h>
int main()
{
    int num;
    printf("enter the number\n");
    scanf("%d", &num);
    float FLOAT= num;
    float half = FLOAT/2;
    // printf("the number is %f",FLOAT);
    
    printf("the half of the number %d is  %f ",num,half);

}